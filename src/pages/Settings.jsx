import React from 'react';
import MainSettings from '../features/Settings/MainSettings';

// Settings and preferences page route entry point
const Settings = () => {
  return <MainSettings />;
};

export default Settings;